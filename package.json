{
  "name": "minime",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "docker compose up",
    "build": "docker compose build",
    "check": "bash scripts/check.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0"
  }
}
